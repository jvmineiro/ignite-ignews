export async function saveSubscription(
    subscriptionId: string,
    customerId: string,
) {
    // Buscar o usuario no FAUNADB com o ID customerId
    // Salavar os dados da subscription no FaunaDB
    console.log(subscriptionId, customerId);
}